# A minimal example.
#
# Runs a task that simply lists the default conda environments.
#
# Usage:
#   sky launch -c min minimal.yaml
#   sky down min

name: minimal

resources:
  cloud: kubernetes
  cpus: 1+

setup: |
  echo "running setup"

run: |
  conda env list
