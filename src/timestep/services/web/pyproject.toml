[build-system]
build-backend = "poetry.core.masonry.api"
requires = ["poetry-core"]

[tool.black]
extend-exclude = '''
# A regex preceded with ^/ will apply only to files and directories
# in the root of the project.
(
  ^/foo.py    # exclude a file named foo.py in the root of the project
  | .*_pb2.py  # exclude autogenerated Protocol Buffer files anywhere in the project
)
'''
line-length = 88
target-version = ['py311']

[tool.mypy]
plugins = ["pydantic.mypy"]

[tool.poetry]
authors = ["Michael Schock <m@mjschock.com>"]
description = "Timestep AI (web)"
name = "web"
packages = [{from = "src", include = "web"}]
readme = "README.md"
version = "3.0.67"

[tool.poetry.dependencies]
agent-protocol = "^1.0.1"
asyncpg = "<0.29.0"
authlib = "^1.2.1"
awscli = {git = "https://github.com/aws/aws-cli.git", rev = "v2"}
databases = {extras = ["asyncpg"], version = "^0.8.0"}
email-validator = "^2.1.0.post1"
fastapi = "<0.101.0"
gql = {extras = ["all"], version = "^3.4.1"}
langchain = "^0.0.339"
llama-index = {extras = ["langchain", "local-models", "postgres"], version = "^0.9.5"}
marvin = "^1.5.5"
minio = "^7.2.0"
pettingzoo = {extras = ["all"], version = "^1.24.1"}
prefect = "2.14.6"
prefect-aws = "^0.4.1"
prefect-docker = "^0.4.0"
prefect-email = "^0.3.2"
prefect-jupyter = "^0.3.0"
prefect-kubernetes = "^0.3.1"
prefect-ray = "^0.3.0"
prefect-shell = "^0.2.0"
prefect-sqlalchemy = "^0.3.0"
pydantic = ">=1.10.8,<2.0"
pyjwt = "^2.8.0"
python = ">=3.11,<3.12"
python-dotenv = "^1.0.0"
ray = {extras = ["serve"], version = "^2.8.0"}
s3fs = "^2023.10.0"
skypilot = {develop = true, extras = ["all"], path = "src/lib/skypilot"}
sqlalchemy = ">=1.4.31,<2"
strawberry-graphql = {extras = ["fastapi"], version = "^0.209.6"}
torch = {source = "pytorch-cpu", version = "^2.1.1+cpu"}
transformers = {extras = ["torch"], version = "^4.35.2"}
uvicorn = {extras = ["standard"], version = "^0.24.0.post1"}

[tool.poetry.group.dev.dependencies]
black = "^23.7.0"
mypy = "^1.5.1"
pylint = "^2.17.5"
ruff = "^0.0.282"
toml-sort = "^0.23.1"

[tool.poetry.group.test.dependencies]
pytest = "^7.3.1"

[[tool.poetry.source]]
name = "PyPI"
priority = "primary"

[[tool.poetry.source]]
name = "pytorch-cpu"
priority = "explicit"
url = "https://download.pytorch.org/whl/cpu"

[tool.ruff]
# Allow unused variables when underscore-prefixed.
dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"
# Exclude a variety of commonly ignored directories.
exclude = [
  ".bzr",
  ".direnv",
  ".eggs",
  ".git",
  ".git-rewrite",
  ".hg",
  ".mypy_cache",
  ".nox",
  ".pants.d",
  ".pytype",
  ".ruff_cache",
  ".svn",
  ".tox",
  ".venv",
  "__pypackages__",
  "_build",
  "buck-out",
  "build",
  "dist",
  "node_modules",
  "venv"
]
# Allow autofix for all enabled rules (when `--fix`) is provided.
fixable = ["A", "ANN", "ARG", "B", "BLE", "C", "COM", "D", "DJ", "DTZ", "E", "EM", "ERA", "EXE", "F", "FBT", "G", "I", "ICN", "INP", "ISC", "N", "NPY", "PD", "PGH", "PIE", "PL", "PT", "PTH", "PYI", "Q", "RET", "RSE", "RUF", "S", "SIM", "SLF", "T", "TCH", "TID", "TRY", "UP", "W", "YTT"]
ignore = []
# Same as Black.
line-length = 88
per-file-ignores = {}
select = [
  "C90", # mccabe
  "E", # pycodestyle (errors)
  "F", # Pyflakes
  "I", # isort
  "N", # pep8-naming
  "W" # pycodestyle (warnings)
]
# Assume Python 3.11.
target-version = "py311"
unfixable = []

[tool.ruff.mccabe]
# Unlike Flake8, default to a complexity level of 10.
max-complexity = 10
